import { StyleSheet } from "react-native";

export default StyleSheet.create({
  inputContainer: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 20,
    width: "100%",
  },
  input: {
    flex: 1,
    borderWidth: 1,
    borderColor: "#0000FF",
    borderRadius: 10,
    padding: 10,
    marginRight: 10,
    backgroundColor: "#fff",
    fontSize: 16,
    color: "#3c096c",
  },
});